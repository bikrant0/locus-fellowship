# Configuration

Git --version

'''bash
git config --global user.name "yourname"

sets your name globally so every commie you make is labeled with its author name

git config --global user.email"<youremail.@example.com>"
--> sets your email globally so Git attaches it to all your commits as your identity

git config --list
--> display all git configuration settings currently applied.

git init
--> Initialize git

git status
--> To check the status

git add
git add .

git commit -m "Message"

git branch -M main

git pull

git push
